/** Tên khoá mang tiền tố miền — xem ghi chú ở `user/user.error.ts`. */
export const CIRCLE_ERR = {
  /** Góc đã đầy — xem `circleCapacity()` */
  CIRCLE_FULL: 'circle.full',
  ALREADY_FRIENDS: 'circle.already',
} as const;
